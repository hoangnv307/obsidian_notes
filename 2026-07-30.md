Một số thắc mắc về thư mục engine/core/datamodel
1. src/engine/datamodel/abstract_metadata_io.* đang để sai vị trí, lẽ ra phải ở phần dataio
2. pixel_pos và geo_pos nên chuyển ra chỗ khác. 
3. data_node.* đang ở sai vị trí
4. thư mục geocoding đang xếp file chưa hợp lý.
5. orbit_state_vector có nên ở thư mục metadata không?
6.  ý nghĩa thư mục node?
7. file band_group, quicklook có nên ở product không?
8. statistic.* có nên ở chỗ raster không?

Phản biện 1: 
- Đồng ý với cách chuyển abstract_metadata_io hiện tại thành xml_metadata_reader, nhưng theo pattern khá tốt của SNAP, vẫn nên có 1 abstract_metadata_io để tạo giao diện chung cho các bộ reader metadata khác. Đồng ý với phương án chuyển sang phần engine/io/metadata. 
- Về pixel_pos và geo_pos, 2 file này đúng là liên quan đến tọa độ, nhưng liệu có nên cho vào datamodel/geometry không?, vì thư mục geometry giờ đang kiểu thuần mô hình hình học. 
- Về phần geocoding, tôi muốn bỏ TiePointGeoCoding đi, vì như SNAP có chia sẻ pattern rằng: 
```
/**

 * A geo-coding based on two tie-point grids. One grid stores the latitude tie-points, the other stores the longitude

 * tie-points.

 *

 * @deprecated since SNAP 8.0.0, Better use {@link org.esa.snap.core.dataio.geocoding.ComponentGeoCoding ComponentGeoCoding} with

 * {@link org.esa.snap.core.dataio.geocoding.inverse.TiePointInverse TiePointInverse} and one of {@link org.esa.snap.core.dataio.geocoding.inverse.TiePointInverse TiePointInverse},

 * {@link org.esa.snap.core.dataio.geocoding.forward.TiePointBilinearForward TiePointBilinearForward} and {@link org.esa.snap.core.dataio.geocoding.forward.TiePointSplineForward TiePointSplineForward}

 */

@Deprecated

public class TiePointGeoCoding extends AbstractGeoCoding {
```
rằng TiePointGeoCoding là một class cũ và nên refactor. Tham khảo SNAP và bổ sung  pâ